export const environment = {
  production: true,
  apiUrl: 'https://api.miapp.com',  // URL para las APIs en producción
  appVersion: '1.0.0',              // Versión de la aplicación (para producción)
};
