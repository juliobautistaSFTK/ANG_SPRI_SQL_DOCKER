{
  "name": "ANG_SPRI_SQL_DOCKER",
  "version": "0.0.1",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",                       // Inicia la aplicación en modo desarrollo
    "build": "ng build",                       // Compila la aplicación para producción
    "build:prod": "ng build --prod",           // Compila la aplicación para producción (con optimización)
    "test": "ng test",                         // Ejecuta pruebas unitarias
    "lint": "ng lint",                         // Ejecuta el linter para verificar el código
    "e2e": "ng e2e",                           // Ejecuta pruebas de extremo a extremo
    "docker:build": "docker build -t ang_spri_sql_docker ."  // Construcción de la imagen Docker
  },
  "private": true,
  "dependencies": {
    "@angular/animations": "^15.0.0",
    "@angular/common": "^15.0.0",
    "@angular/compiler": "^15.0.0",
    "@angular/core": "^15.0.0",
    "@angular/forms": "^15.0.0",
    "@angular/platform-browser": "^15.0.0",
    "@angular/platform-browser-dynamic": "^15.0.0",
    "@angular/router": "^15.0.0",
    "rxjs": "~7.8.0",
    "tslib": "^2.5.0",
    "zone.js": "~0.11.5"
  },
  "devDependencies": {
    "@angular/cli": "^15.0.0",                // CLI de Angular para interactuar con la app
    "@angular/compiler-cli": "^15.0.0",       // Compiler CLI de Angular para el build
    "@angular-devkit/build-angular": "^15.0.0",  // Paquete necesario para el builder del proyecto
    "@types/node": "^18.11.18",                // Tipos para Node.js
    "typescript": "~4.8.4"                     // Versión de TypeScript compatible
  }
}
